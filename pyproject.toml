[project]
name = "backlog"
version = "0.0.0"
description = "Self-hosted to-do management REST API built with FastAPI and PostgreSQL"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.135.1",
]
